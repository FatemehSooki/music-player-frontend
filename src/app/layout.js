import localFont from "next/font/local";
import { GiMusicSpell } from "react-icons/gi";
import Sekuya from '@/lib/fonts.js'
import "./globals.css";
import PlayerProvider from "./context/PlayerContext";
import MusicPlayer from "@/components/MusicPlayer";
import Developer from "@/components/developer";
import Search from "@/components/Search";

const Outfit = localFont({
  src: "../fonts/Outfit-Regular.ttf",
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className='h-full antialiased'
    >
      <body className={`${Outfit.className}  min-h-full flex flex-col`}>
        <PlayerProvider>
          <div className='w-full flex justify-center items-center gap-3'>
            <GiMusicSpell className='text-[#1DB954] text-3xl sm:text-4xl md:text-5xl lg:text-[50px]' />
            <h1 className={`${Sekuya.className} text-[#1DB954] text-4xl sm:text-5xl md:text-6xl lg:text-[60px] text-center bg-zinc-950 font-bold`}>SFATIFY</h1>
          </div>

          <div className="absolute z-50 left-3 sm:left-4 md:left-6 top-14 sm:top-16 md:top-20 -translate-y-1/2">
            <Developer />
          </div>

          <Search />

          {children}
          <MusicPlayer />
        </PlayerProvider>
      </body>
    </html >
  );
}